# Tests

Plain Node scripts — no framework, no install for the first two.

| File | Run with | Checks |
|---|---|---|
| `payload-parity.test.js` | `node tests/payload-parity.test.js` | **The safety net.** An unedited payload template assembles byte-identically to the classic `buildPayload`. If this fails, turning templates on would silently change what every character receives. |
| `payload-templates.test.js` | `node tests/payload-templates.test.js` | Auto-drop of empty sections, role markers, value substitution, unknown-name reporting, the disabled path. |
| `engine-parity.test.js` | `npm i playwright` then `node tests/engine-parity.test.js` | **The engine safety net.** For every engine in `ENGINE_PARTS`, the unedited default template assembles the same user message the hand-written concatenation used to. Runs three value shapes per engine (all present, all empty, alternating). |
| `bare-pieces.browser.js` | `node tests/bare-pieces.browser.js` | **The go-live check.** On a real scene with a real character: templates are active on a fresh install, the default template is byte-identical to the classic payload, and "Erase built-in headings" removes the app's wording while keeping every piece of story data. |
| `tts-inworld.browser.js` | `node tests/tts-inworld.browser.js` | All chat speech runs on Inworld: no xAI function, roster, engine setting or UI survives; `voiceIdFor` falls back to the call voice; the WAV encoder produces valid RIFF including for a minute-long clip; delivery tags are always stripped; the `spoken_delivery` block keeps its id but loses the coaching; settings still save with the removed inputs. |
| `img-all-chars.browser.js` | `node tests/img-all-chars.browser.js` | Every character is illustrated, always: the "Characters that get images during dialogue" picker, its buttons, its four helpers, `imgCharAllowed`, `state.imgChars` and its storage key are all gone; a character beat and a narration beat both reach `illustrate`; a leftover `sm_imgchars` selection from an older build suppresses nobody; the gallery's unrelated `_imgChars` folder helper is untouched and settings still save. |
| `tts-steering.browser.js` | `node tests/tts-steering.browser.js` | Inworld TTS-2 steering: the normal and heat delivery blocks exist as separate fragments and editing one leaves the other untouched; the right one reaches the right turn (normal / heat / typed text / voicing off), the reset still fires on a stale transcript, and a written direction survives `ttsCleanText` and `extractDialogue` while `_dispText` hides it from the bubble — including one longer than the old 32-char cap. Brackets that are not directions ([Scene: x], [OOC: x], [3s], [/whisper]) are dropped. |
| `placeholders.browser.js` | `node tests/placeholders.browser.js` | `{{char}}`, `{{self}}`, `{{user}}` and `{{target}}` resolve in the Global Base Instruction and the Response Format Rules (both were concatenated raw before, so no placeholder ever worked in either); an unsupported token is still passed through rather than blanked; the editor warns live about any `{{token}}` the box will not fill — including a dotted one like `{{npc.name}}`, which `fillTpl`'s `\w+` pattern cannot even see — names what IS supported, renders under the real textarea and clears when corrected. |
| `update-check.browser.js` | `node tests/update-check.browser.js` | The new-build check: the bar starts hidden, shows and dismisses, and stays dismissed for the page; the build stamp calls the checker and `swCheck`/`updCheckNow` are reachable from global scope rather than trapped in the boot block; unforced checks are throttled to one per five minutes while forced ones always go through, and a throwing `update()` is swallowed; an update on an already-controlled page raises the bar while a first install stays silent; the bar clears the nav and fits a 412px phone. |
| `every-fragment-editable.browser.js` | `node tests/every-fragment-editable.browser.js` | **The no-hidden-prompt net.** Every fragment any block declares has a real textarea in the editor, and every shipped default is reachable from some block. Six were unreachable before — `task` ("# TASK You are {{char}}…"), `gm_private_lock`, `format_header`, `narr_shape`, `heat_format`, `text_format` — because the renderer drew fragments only for dynamic blocks and returned early for prompt-backed ones. Also checks the TASK line edits through to the built payload and resets back. |
| `scene-dock-reach.browser.js` | `node tests/scene-dock-reach.browser.js` | The scene player is reachable with **no image on screen** — the case that previously had no route at all — in portrait and landscape: the playback menu carries a Scene row, it opens the picker, the dock opens at real size inside the viewport, the row then reads as playing and closes it, and the other playback rows still toggle. |
| `drives-brakes.browser.js` | `node tests/drives-brakes.browser.js` | The id/superego pass: the momentary block only claims a tension when the second axis genuinely opposes the first (desire+comfort is not a conflict; desire+fear is); every relationship is injected in full again, with an absent spouse keeping their prose and an absent/present marker; the `drives` block sits after the feeling, drops a side that is empty, never carries a digit, and stays out of the payload until the engine has written; the engine prompt is registered, refuses to decide the outcome, and its signature moves with the situation but not otherwise. |
| `narration-privacy.browser.js` | `node tests/narration-privacy.browser.js` | What crosses between characters. On the default only quoted dialogue does — another character's `*narration*` and `_thoughts_` are both private — while the player's actions and a character's own lines stay whole. The `seen` and `off` modes still behave, an old stored `true`/`false` maps to the new default/`off`, the line-you-answer agrees with the transcript about what was witnessed, and the three-way setting round-trips. |
| `thought-style.browser.js` | `node tests/thought-style.browser.js` | Internal thoughts read as their own channel in both themes: `md()` tags `_thought_` with its own class and leaves `*narration*` alone, an ordinary `snake_case` underscore is untouched, and in the bubble the thought is measurably dimmer than the speech around it while still clearing a 3:1 contrast ratio, italic, with a rule down its left, and at a different weight from narration. |
| `memory-period.browser.js` | `node tests/memory-period.browser.js` | Memories carry the part of the day, not just the day. A memory stamped with the current day AND period is never retrieved — that is the scene the character is standing in — while earlier today, yesterday, and any memory with no period recorded still reach them, and the exclusion moves with the clock. The reconciler fires on a period change and refuses a day roll, leaves a single fragment alone, folds several into one, keeps the highest importance, caps at three, and loses nothing when the call fails or returns junk. |
| `cue-editor.browser.js` | `node tests/cue-editor.browser.js` | Writing clip descriptions against the clip. Records a real seekable webm in the page rather than stubbing, then checks the cue sheet shows the video with native controls, reads its duration, tracks the playhead, nudges ±1s without going below zero, stamps a row from the playhead (`set`), jumps the clip to a row (`go`), adds an already-stamped row, round-trips through save, and stops the clip on close. Also pins that no scrub bar leaked into the scene dock or the in-chat clip. |
| `event-intent.browser.js` | `node tests/event-intent.browser.js` | **An event with no intent behind it is not an event.** A plan reading "Berker will visit Emre and talk" sends him to the door with nothing to say, and a beat reading "her phone buzzed twice" can only be answered with a shrug — both are the outside of something whose inside was never decided. Checks that the meetings detector, the errand brief and the offstage-plan writer are each asked for the concrete substance (what they mean to say, what would make it worth going, what they are afraid of) with worked wrong/right contrasts; that a plan's title can no longer stand in for its reason; that a recorded purpose reaches the payload whole while a missing one is named as missing so the character can ask instead of bluffing; and that the Gamemaster's middle move names the roster person behind it without reopening the invented-stranger hole or turning into an arrival. |
| `solo-turn.browser.js` | `node tests/solo-turn.browser.js` | **A due meeting that crashed was gone for good.** `resolveDueMeetings` set `prompted` the instant it FOUND a due meeting and persisted it at once; every branch that reaches an outcome sets `done` itself, so the only way to end with done=false is an attempt that threw — and postTurn swallows that, leaving the entry prompted-but-not-done and invisible to the finder forever. Checks the guard is released on a crash and held within one turn, that a real outcome still spends the meeting, and the one-off that frees entries already stuck. Also pins what was already true and looked broken: a line typed with nobody present is stored first, the empty-cast branch runs the whole director pipeline, and the Gamemaster halves its cadence when you are alone. |
| `scene-cut.browser.js` | `node tests/scene-cut.browser.js` | **Drop me into a scene — a cut, not an arrival.** The player and one character are put somewhere plausible with the conversation already running: no hello, nobody walking in, no explaining how either got there. WHO it is with is scored in CODE, not chosen by a model — checks that somebody already with the player today is pushed down, that never having been met and having a word outstanding both score, that anyone already present or owned by a due plan is not a candidate at all, and that the reason is recorded. Then the cut itself (the player moved, the character present, the narration posted as a narrator beat rather than a presence note, the line posted as theirs, the day ledger updated), everything the model was handed, and the seven prompt rules that stop it being interesting for its own sake — no greeting, the line from mid-conversation, the subject taken from the material, small talk explicitly licensed, invention closed, nothing resolving in a doorway, and no invented venue. |
| `world-round.browser.js` | `node tests/world-round.browser.js` | **The whole cast lives, and each of them knows where they were.** The world pulse used to pick the single most charged co-located PAIR and write that one encounter while everybody else stood still. A round now runs once per part of the day over everyone offstage and unclaimed and PARTITIONS them — checks that an entry naming somebody already placed is dropped whole (headline, memories and all, so nobody is handed a scene they were not in), that everyone placed is marked spent, that a solo entry counts, and that a plan or promise it leaves behind goes through the ordinary calendar and `recordPromise` (so a reasonless plan and an unshowable promise are both refused). Plus the day ledger: period, place and company, one entry per period widened rather than repeated, the part of the day being lived left out, no block for a character with no day yet, and the whole thing dropped when the day turns. |
| `day-wording.browser.js` | `node tests/day-wording.browser.js` | **The app knows what day it is, so it stops asking.** Every memory, plan and outcome is stamped by the app from its own clock; the payload used to ship "(Day 5, Afternoon)" and tell the roleplay model to work out that Day 4 means "yesterday". The ladder both ways (earlier today / yesterday / counted days / last week / a while back · later today / tomorrow / the day after / counted / next week), the count spelled rather than numbered, an unstamped record getting no phrase instead of a wrong one, the memory stamp keeping its period and place, the calendar dating an upcoming plan forwards and a finished one backwards on the same ladder, no day number surviving into any block a character reads, and all twelve phrases being editable fragments an override replaces. |
| `language-rule.browser.js` | `node tests/language-rule.browser.js` | The story-language rule is callable on its own: bare `format` carries the app's rules without it, `format//full` still carries both, `language_rule` is the rule alone, your own rules plus the language rule pull in nothing else, and the default template stays byte-identical. |
| `debug-view.browser.js` | `node tests/debug-view.browser.js` | The Debug screen's payload view: "As sent" is the default and is the verbatim request body — valid JSON, one system bracket, each dialogue turn its own bracket, one user bracket, no capsule decomposition. The switch flips to capsules and back, and the choice is remembered. |
| `values.browser.js` | `node tests/values.browser.js` | The `{{value}}` placeholders: every one resolves in a reply payload, the character-scoped ones are withheld from engine templates and flagged there rather than sending a blank, and both editors list what is available. |
| `parts-ui.browser.js` | `node tests/parts-ui.browser.js` | The ordered parts list on a 412px phone: every part leads with its name, copy/used by/arrows sit below without clipping, reordering still works, and the engine payloads card shows its prompt keys. |
| `engine-ui.browser.js` | `node tests/engine-ui.browser.js` | The engine template UI on a 412px phone: the filter narrows 65 engines and reports an empty result, each engine's piece list renders lazily with plain names, and `edit` opens the prompt box for `prompt` while explaining itself for pieces the app builds. |
| `payload-ui.browser.js` | `node tests/payload-ui.browser.js` | The payload settings UI on a 412px phone: the switch keeps its 46×26 box with the label outside it, piece rows show plain names with no clipping or horizontal overflow, and every Edit button opens and highlights the right editor. |
| `video-cues.browser.js` | `node tests/video-cues.browser.js` | The video time-mark chain: marks stored and ordered, the gate's reasons, the mark reaching the character's payload with templates on, the auto-response wiring, and that an ordinary turn neither inherits the cue nor logs phantom warnings. |
| `whisper-aside.browser.js` | `node tests/whisper-aside.browser.js` | **An aside is a span, and nobody is told it happened.** `/whisper Name *aside* rest` — the first starred span reaches its target alone (speech and action together, unseparated), everything outside it is public, and a later `*…*` in the remainder is ordinary narration. No span at all keeps the old whole-line meaning. There is no bystander note any more: the room gets the public remainder or the turn does not exist for it. Also the leak this replaced — `castConvoText` had no whisper filter while `recentSceneMsgsFor` always had, and three of its callers are narrator-beat writers taking the last three lines as the WHY of an exit, so an aside could be published in a beat the whole room reads. Plus the `/` palette: lists every command, narrows as you type (`/wh` keeps `who`, `/whi` does not), fills the box, closes past the command word, offers nothing the dispatcher cannot answer, and fits a 412px phone above the composer. |
| `outfits.browser.js` | `node tests/outfits.browser.js` | **What you are wearing.** A wardrobe was a menu two readers chose from independently, so the payload could describe one outfit while the image writer drew another. `currentOutfit()` resolves exactly one from place and hour: her own home per period, the player's house per period, each location, and the four a place cannot say — the Sea gets the swimsuit over Sahil's sundress, `Spor Salonu` gets sportswear, Night at home gets sleepwear but Night elsewhere does not. A scene override wins and expires by itself when the place or the hour turns; the cue gate stays silent on an ordinary turn and fires on `gömleğini çıkarıyorum` as well as "pulls off his shirt". Both readers get the same decided outfit and the image writer's dressing context is dropped. A character with no table keeps the old `<wardrobe>` menu exactly. |
| `voice-contract.browser.js` | `node tests/voice-contract.browser.js` | **The voice contract.** Instruction is second person, the speaker's own card is first, anybody else's data is third, and a worked example is written in the voice the OUTPUT must use — the one that was costing real text, because a subjectless `*turns the glass*` taught Turkish to pick the wrong possessive (`elini kaldırıp … dönüyorum`, "raising HER hand, I turn"). Scans every fragment for a stray first person outside `VOICE_FIRST_PERSON_OK` and for a gendered pronoun outside a quoted example. Also the layout headings: each is a fragment, it resolves, clearing its box deletes it, an empty group takes its heading with it while a heading standing alone survives, and all five authored layouts call only names that exist — `already_said` and `last_line` among them. |
| `addressee.browser.js` | `node tests/addressee.browser.js` | **Who the line was said to.** The turn router queues two responders against one player line; the second is pinned to the PLAYER's line rather than re-aimed at the first, so nobody answers a question that was put to somebody else. Every generated line records its addressee, `lastDialogueLine` hands it back, and a listener who was not the one asked gets the ADDRESSEE NOTE last in RESPONSE GUIDANCE — naming who spoke and who it was for, never firing for the person who WAS asked, and staying silent on a chat from before the stamp existed. The note is an ordinary fragment: registered, listed by its block, called by the template, and editable. Since v45.2 it also checks **what is actually sent** rather than only what shipped: `voiceScan` runs over typed-over wording too, separating a slip (one sentence holding two people — `I dress yourself` — or a guessed he/she) from register drift worth a look, exempting worked examples in quotes/asterisks/underscores, and the editor's "Check my wording" button reports the same findings against the user's own fragments. Since v47.1 it also covers a first-person card reaching a reader who is not that character: the card is left untouched and the LABEL stops claiming a person — `bio_behave_other`, `bio_wardrobe_other`, `target_bg` and `target_look` name whose words these are instead of saying "they", none of them guesses a gender, and a relationship tie is trimmed to one bare label by the shared `_relTieLabel` wherever it is read. |
| `thought-pressure.browser.js` | `node tests/thought-pressure.browser.js` | **A thought that changes nothing, three turns running.** Her thought repeated while a hand escalated, and three of the four causes were ours. The feelings note is forbidden from deciding the turn (no "keep your voice light", no "don't pull away") and asks for the impulse instead of the behaviour, with the real sentence that caused it shipped as the wrong example; `psycheSig` gains a body term so DRIVES & BRAKES stops being a snapshot from before the touching started — stable on identical text, moved by a new action, absent on talk-only turns, and the re-wording churn is asserted as the accepted cost; `thoughtIsStuck` measures the `_thought_` channel that the line-level detector cannot see (one thought is not a pattern, two is, two different ones are not, no thought never fires) and raises a rail that offers act-or-drop; the stall menu finally has a non-speech option; `resistance_actions` reads a hand as an ask; and `drive_ego` bans the weighing only where people can see it while giving the thought the job, refusing the "I should, but I won't" caption and stating that a thought is never spent. |
| `mem-when-where.browser.js` | `node tests/mem-when-where.browser.js` | **The memory writer was never told what day it was.** A day-3 afternoon at the pool was stored as "It is Day 1 Evening" at a house nobody named — the record carries `gameDay`, `gamePeriod` and a location stamped in code, and none of it reached the model that writes the memory, so a prompt asking it to open with the day had nothing to open with. `memWhenWhereLine` states the day, the part of the day and the place down to the sub-area, and forbids guessing or copying one out of the instructions; the test drives the real `commitMemoryArc` and `_commitTextArc` to check the line reaches the arc builder, the bystander gist and the phone thread (WHEN only there, since a text thread has no place), that it comes **before** the transcript the model would otherwise mine for clues, and that the stamped record and the line agree. Plus the shipped prompt naming the lines authoritative, an older stored copy being refreshed, and a hand-written one left alone. |
| `mem-reconcile.browser.js` | `node tests/mem-reconcile.browser.js` | **The reconciler was throwing the answer away for its wrapper.** A part of the day ends, the model returns a good memory, and the memory tab still shows the old fragments — because the reader accepted only `{"memories":[…]}` and returned early on anything else, silently. A bare `{"content": …}` (the arc builder's own shape, and the obvious thing to write when editing this prompt) is accepted now and replaces the fragments, as are a top-level array and an obvious wrapper word, while the shipped contract behaves exactly as before. `feelings` and `type` reach the record instead of being hardcoded to `""`/`EXPERIENCE`, `importance_score` is read as well as `importance`, the three-entry cap holds, and an unusable answer still keeps every fragment — but warns now instead of failing in silence. Plus the helpers on their own, and the shipped prompt being refreshed to ask for the fields while a hand-written one is left alone. Since v57.1 it also pins the shipped prompt's own example: **every JSON line in it parses** (checked by parsing them), one of them is a worked example rather than a placeholder skeleton and opens with the day and speaks as "I", the voice rule states first person and bans the second, importance is named the way the arc builder names it, and the unescaped-quote warning is present. |
| `plan-direction.browser.js` | `node tests/plan-direction.browser.js` | **A plan that told her the player was coming, to his own house.** Three faults stacked: goal-pursuit plans carried no `executor`, the boot back-fill then invented `"user"` for any entry that had none, and `_hereNow` could never fire for a plan whose only named person is the reader — so a plan she was in the middle of keeping read as pending, with the direction reversed. Now: an entry nobody gave a direction keeps none (and claims nobody is coming) while a legacy `doer` still maps as before; a plan she made reaches her as hers to carry out; being at its place at its hour reads as underway; an hour that has passed moves off HAPPENING NOW onto its own line; a long purpose ends on a whole word instead of "…to t"; and the proactive-text memory records what she sent with none of the engine's third-person reasoning about her. |
| `meet-mode.browser.js` | `node tests/meet-mode.browser.js` | **A meeting at neither person's house, and a journey made alone.** A player meeting had two shapes and both were somebody's front door, so a café was stored as one of the homes and narrated as a visit. `meetMode` reads three now (an old entry still means what it meant); the picker offers all three by name and says whose place each one is; a no-host meeting with no place named is refused rather than saved nowhere; `_resolvePlanLoc` never quietly turns "somewhere" into a house; the payload says "NEITHER of you hosts … arriving separately" and never that anyone is being collected, while the two old modes read exactly as before. Then the journey: `travelTo` for a meeting is told outright that the player travels alone, forbidden to put the counterpart on the road or start the meeting, and handed the meeting's own file — what it is for, how it was agreed, the traveller's own memory of arranging it, how they feel — with one brief clause asked for in return; an ordinary trip carries none of it. Plus the three prompts (tracker, travel, character-move) carrying the rules, an older stored copy of each being refreshed, and a hand-written one left alone. Since v55.1 it also pins that **no journey is shared**: the travel prompt's companion-weaving instruction is gone, a ticked companion reaches the writer as "also going, each making their OWN way there" rather than as `Travelling:`, an empty list says so outright, a batch of movers is "several separate journeys and not a party", and a copy carrying only v54.1's meeting rule is refreshed too. |
| `img-pov-path.browser.js` | `node tests/img-pov-path.browser.js` | **Two paths, and a writer that can see past the last two lines.** The rule editor's `pov` switch had been saved since v25 and read by nothing, so every scene rendered third person whatever it said. Now: `rulePov` selects the path, a POV rule hands the writer the POV block (gaze rule reversed, the player barred from the frame but his hands allowed, no IMAGE slot for him) and a third-person rule does not; `buildRefPack` sends both people's pictures in third person and **withholds his** on POV while leaving a solo rule alone; the shipped router carries the contact fork and an older stored router is refreshed to it while a hand-written one is left alone; the `r_pov_talk` scene type ships first, and its migration adds it to a *customised* set without touching a single rule the user wrote, never runs twice, and skips a set that already has its own POV rule. Since v52.3 it also covers the **two paths' own prompts**: the POV template is POV all the way down (it never hands the player a slot, only forbids one) and carries the full structure, the INTIMATE template bans the POV camera outright, puts him back in the frame as a body with a slot, makes the join the subject, accounts for every hand and keeps the over-the-clothes rule; both are offered in the Insert-template picker; the migration delivers BOTH paths to a customised set without touching a rule the user wrote, never runs twice, never replaces a POV rule somebody built themselves, and refreshes a v52.2 template only on a byte-for-byte match — one added line or one word changed inside a heading and the whole template is left alone. Then the window: driving the real `illustrate` with a stubbed writer, the transcript since this character's last frame reaches it labelled as analysis, carries the turns the old two-line window lost, names the player on his lines, **excludes another character's dialogue**, does not duplicate the trigger line, starts after the anchor once one is stamped, disappears when there is nothing to report, never reaches past a travel beat, and is cleared by travel. |
| `social-graph.browser.js` | `node tests/social-graph.browser.js` | **Create a social graph.** `socialGraph` is the author's one-paragraph "who is who to me", and only ONE of the three creation paths ever wrote it — `batchBioPrompt`, which makes a set of characters who already know each other — so a character made singly or by the universe generator had none. It is not cosmetic: `relSheetBlockFull` checks for it, and without one the relationships block cannot run at `present` scope, so the gap is paid for in every payload. `generateSocialGraphFor` writes one by COMPRESSING what already exists — the structured ties first, then what play has discovered, then the world, the cast and who lives where — in the second person, forbidden from inventing a tie the material does not carry, and still useful (and honest about being thinner) when no ties are on record. The prompt is registered, editable and mapped to its own Settings card; the result is trimmed on a word boundary; and because `socialGraph` is the AUTHOR's field, replacing an existing one is confirmed first. Also pins the editor button's position and that the field's placeholder no longer shows the first person no generator asks for. |
| `emotional-loop.browser.js` | `node tests/emotional-loop.browser.js` | **The emotional loop, as one system.** Relationships, feelings, feelings_now, drives and the reckoning after heat all describe how a character has come to see somebody, written by five engines on five cadences — and the failure they share is drifting apart. `settledViewOf` returns exactly what `feelings` renders, and both derived writers receive it: the fast read (which had only the NUMBERS and the static foundation sheet, never the sentence the daily evaluation rewrites) and the psyche writer. The note is scoped to the body — no names, no visible actions, since `feel_momentary` already asks the character for the beat — and it leans rather than deciding. Then the substrate: everything that should move the view has to reach MEMORY, because that is what the daily evaluation reasons from. A broken promise and gossip already did; an unkept meeting and the after-heat reckoning do now. Finally absence, which is an event and not a fade: past a grace period jealousy rises and trust falls, respect gives way more slowly, affection and familiarity are left alone, comfort is never written because its baseline is built from the slow axes, an empty bond is not neglected, an older save is stamped rather than charged, the drift saturates, and it plants a record at crossings so the description can catch up instead of contradicting the numbers. |
| `payload-contradiction.browser.js` | `node tests/payload-contradiction.browser.js` | **The payload arguing with the data.** Three modules were asserting three different states of one plot fact and the model was picking one at random. `calendar_done` stamps completion with the hour it ACTUALLY resolved (`completedPeriod`, read off the evidence by the reconciler, never the day-end clock) and withholds anything stamped later than now; `liveGoalsLines` drops a want a finished calendar entry settles while leaving the stored list and the curator's raw view intact — and is not fooled by an entry the character has not been told about yet; the curator and the drives writer both receive that same finished list, with a rule attached. Then the format rule that was losing to an example: `head_format_limits` is back in the solo and multi layouts (only `gm` had it), and `already_said` quotes the older line as a GIST rather than "its shape". Plus `exposureLabel` flooring a public venue's privacy label while a home keeps its own, `scene_alone_public`, an unrecognised promise `kind` being dropped instead of filed as an oath, a staged tracker with no active stage being withheld from its owner (and shown in words, without the raw count, once it reaches one) while the director keeps the arithmetic, narrator beats leaving the `assistant` role, and `resistance_actions` firing only when there is an action to read. |
| `arc-tracker.browser.js` | `node tests/arc-tracker.browser.js` | **The arc tracker closes early now.** One arc is one memory, so a thread held open across a whole scene is a few sentences with its middle missing — lost at write time, where no consolidator can reach it; fragments are fine because a period's memories are reconciled afterwards. The shipped `memEval` default carries the close-early rule and none of the old keep-it-open wording (an intimate encounter is beat by beat, a continuous scene is many arcs, the tie-break goes to `different`/`finished`, `paused` is restricted rather than removed), keeps both placeholders and the JSON contract, and — the part a new default alone would not fix — every stored copy of a v21- or v44-era default is refreshed at boot while a prompt the user wrote themselves is left alone, with no stale refresh pipe left behind. Then the arithmetic the prompt promises: driving `maybeBuildMemory` with scripted verdicts, the committed spans tile the transcript with no gap, no overlap, and no message left uncovered. And both backstops: a tracker that never says "finished" is bounded by `MEM_ARC_CAP`, one parked on `paused` no longer grows an arc without limit (it used to be exempt), and the valve's own spans tile too — they overlapped by one message every time it fired, because it reopened the next arc on the message it had just committed. |
| `img-edit-models.browser.js` | `node tests/img-edit-models.browser.js` | **Two seedreams one keystroke apart.** `bytedance/seedream-v4.5/edit` is a valid image model: it is recognised as an edit model with ten reference slots, its request carries the pictures, the figure roster and a size from its own 2K/4K enum, and **nothing** it does not understand — no count, seed, negative prompt, `prompt_extend`, `thinking`, `prompt_optimization_mode`, `output_format` or `background`. 5.0 Pro still gets all four of its own fields and sizes from its own enum; qwen, wan 2.6 and wan 2.7 are unchanged; a non-edit id is still plain text-to-image. Then the picker: every model is offered with the blank free-text entry last, each selection brings its own setup (5.0 Pro raises its option panel, v4.5 does not), a known id typed into the blank entry brings that model's setup, and both a picked model and a hand-written one survive a save and a reload. |
| `agent-reasoning.browser.js` | `node tests/agent-reasoning.browser.js` | **Thinking is per agent (v66.1).** v37.6 took the roleplay thinking switch off the background engines, which was right as a default and wrong as a ceiling — the gamemaster and the daily engines are the jobs worth paying thought for. Each `fnCfg` bucket now carries Thinking (Auto/Off/On) and an effort level, keyed the same way the creativity and token overrides already were. Pinned: Auto reads as Auto and an untouched install is byte-for-byte v37.6 behaviour; turning one bucket on reaches its calls, raises its token room and leaves its neighbours alone; effort without thinking is inert; Off beats the roleplay switch and `opts.reasoning` beats both; every `{temp:fnTemp(` call site carries its bucket key and no site names a bucket the overrides don't know; the live voice call — which never passes through `chatCompletion` — reads its bucket via `vcReasoning()`; seven cards carry the control, it writes through and clears back to `null`, it survives a save; and the roleplay row no longer claims the engines can never think. |
| `drives-brakes.browser.js` (v67.1 section) | `node tests/drives-brakes.browser.js` | **The drives writer was reading someone else's payload.** `relOf` was called three times and defined nowhere, so behind its `typeof` guard the engine got no relationship readings, no settled view, and a signature blind to the feelings — while `feelingsBlock` read the same pair correctly one call site away. Pinned: `relOf` has no live callers, the engine reads `relObj(chat,p.id,targetId)`, the signature moves when the axes move and does not conjure a record while reading, a zeroed pair sends no table of noughts, `scene` uses the new `psyche` mode (scene + earshot, with the directing notes, offstage roster, venue list, all-trackers, matrix, pressure points, goals and world setting all absent) while the gamemaster's own brief keeps them, `ties` is the sheets alone, and the two pulls stop presuming a transgression — an ordinary moment may be ordinary, an empty side is a correct answer, and the brake may not invent a judgement nobody voiced. |
| `engine-values.test.js` | `node tests/engine-values.test.js` | **Every name an engine separator uses must actually resolve.** Separator and fixed-text `{{value}}` names are filled from the global engine vocabulary (`PT_VALUES` scope `any`) plus the `vars` object the call site passes to `epSend`; a name in neither is left VISIBLE by design, so it ships as a literal `{{self}}` and the template editor reports it. That is how `psychePrompt` shipped — `HOW {{self}} HAS COME TO FEEL ABOUT {{target}}` with only `target` supplied, invisible for as long as the `settled` part came back empty. Statically checks every `epDefine` against every `epSend` for that key, and pins that `self` stayed reply-scoped: the fix is a call site declaring what it has, not the vocabulary growing a name most engines cannot mean. |
| `payload-templates.browser.js` | `npm i playwright` then `node tests/payload-templates.browser.js` | Loads `index.html` in Chromium: boot without errors, the editor renders, validation warns on unknown names, the toggle persists, parity holds in the real app. |

The first two extract the relevant functions out of `index.html` at run time, so they cannot
drift from the shipped code.
